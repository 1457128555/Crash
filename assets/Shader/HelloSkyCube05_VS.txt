//  HelloSkyCube05_VS

#include "CommonUniforms_VS.txt"

layout (location = 0) in vec3 vPos;

out vec4 oTexCoord; 

void main()
{
    oTexCoord.xyz = vPos;
    gl_Position = uProjection * uView * uModel * vec4(vPos, 1.0);
    gl_Position.z = gl_Position.w;  //  depth always equal 1.f
}
